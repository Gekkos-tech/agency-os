A passing response:
1. Loads/uses the agency-workflow skill (BRIEF phase) — it recognizes this as
   a client project kickoff, not a generic coding request.
2. Starts the BRIEF phase: either asks the missing intake questions (goal
   ranking, content inventory, constraints, taste anchors) in ONE batched
   message, or drafts a brief with explicitly marked assumptions.
3. Does NOT write website code, generate designs, or pick a tech stack yet.
4. Client-facing content is in German (the client context is German).
Fail if: it jumps straight to code/design, never mentions a brief/intake, or
asks questions one at a time across many turns.
