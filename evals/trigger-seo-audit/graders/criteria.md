A passing response:
1. Uses the seo-audit skill as the single entry point — NOT seo-technical,
   seo-content, seo-local or several SEO skills at once for this request.
2. Follows the audit process shape: crawl/render, business-type detection,
   specialist delegation (subagents if available), aggregated health score,
   prioritized action plan.
3. Output artifacts match the skill contract ({domain}-audit/ structure or an
   explanation of what would be produced).
Fail if: multiple competing SEO skills are loaded for the same request, or the
response is generic SEO advice with no audit process.
